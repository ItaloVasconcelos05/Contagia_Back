# ================================================================= #
#  ARQUIVO .GITIGNORE PARA O PROJETO GLOBO-RESIDENCIA (BACKEND)     #
# ================================================================= #

# 1. Dependências
# A pasta node_modules é gerada pelo pnpm/npm e contém todos os pacotes.
# Ela nunca deve ser enviada para o repositório.
node_modules/

# 2. Código Compilado (Build)
# A pasta 'dist' contém o código JavaScript gerado a partir do TypeScript.
# Como ela é gerada pelo comando `pnpm build`, não precisa ser versionada.
dist/

# 3. Variáveis de Ambiente e Segredos
# O arquivo .env contém chaves de API e outras informações sensíveis.
# ESSENCIAL ignorar para segurança.
.env
.env.local
.env.*.local

# 4. Arquivos Temporários e Uploads
# Pastas usadas para armazenar arquivos durante o processamento.
# Elas são específicas do ambiente de execução.
tmp_audio/
uploads/

# 5. Logs
# Arquivos de log gerados durante a execução ou depuração.
*.log
npm-debug.log*

# 6. Arquivos de Saída de Testes
# Arquivos gerados por comandos e testes, como o da sua requisição.
response.json

# 7. Arquivos de Sistema Operacional e Editores
# Lixo gerado pelo Windows, macOS e editores de código.
.DS_Store
Thumbs.db
.vscode/
.idea/